"""Backend command handlers grouped by product domain.

The top-level :mod:`torque.server` module remains the application composition
root and compatibility facade.  Command modules own request validation and
response construction for one cohesive command family.
"""
