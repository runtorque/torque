---
description: Show all Loom agents and what they're working on
allowed-tools: mcp__loom__weaver_agents_list
---

List all active agents using the weaver_agents_list MCP tool.

Format the output as a table with columns:
- Agent name and group
- Status (idle / active / error)
- Current task (if any)
- Activity detail (if any)

Highlight agents that need attention (error status or blocked tasks).
